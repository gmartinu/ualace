import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#43A047',
    },
    secondary: {
      main: '#7C9F32',
    },
    error:{
      main: '#E53935',
    }
  },
});

export default theme;
