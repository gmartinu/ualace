import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#922C40',
    },
    secondary: {
      main: '#1E2640',
    },
    error:{
      main: '#E53935',
    }
  },
});

export default theme;
